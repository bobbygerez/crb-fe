export const setRoles = ({ commit }, payload) => {
  commit('setRoles', payload)
}

export const setRole = ({ commit }, payload) => {
  commit('setRole', payload)
}

export const setRoleParentId = ({ commit }, payload) => {
  commit('setRoleParentId', payload)
}
