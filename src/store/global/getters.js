export const leftDrawer = (state) => state.leftDrawer

export const breadCrumbs = (state) => state.breadCrumbs

export const cart = (state) => state.cart

export const totalCart = (state) => state.totalCart

export const cartLength = (state) => state.cartLength

export const qty = (state) => state.qty

export const provinces = (state) => state.provinces

export const cities = (state) => state.cities

export const brgys = (state) => state.brgys

export const delCities = (state) => state.delCities

export const delBrgys = (state) => state.delBrgys

export const serverPagination = (state) => state.serverPagination

export const page = (state) => state.serverPagination

export const perPage = (state) => state.serverPagination

export const filter = (state) => state.filter
