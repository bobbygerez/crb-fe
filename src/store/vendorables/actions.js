export const vendorables = ({
  commit
}, payload) => {
  commit('vendorables', payload)
}

export const vendorable = ({
  commit
}, payload) => {
  commit('vendorable', payload)
}

export const vendorablePivotRank = ({
  commit
}, payload) => {
  commit('vendorablePivotRank', payload)
}

export const vendorablePivotDisPercentage = ({
  commit
}, payload) => {
  commit('vendorablePivotDisPercentage', payload)
}

