export const setMenus = (state, payload) => {
  state.menus = payload
}

export const setMenu = (state, payload) => {
  state.menu = payload
}

export const setSearchMenu = (state, payload) => {
  state.searchMenu = payload
}
